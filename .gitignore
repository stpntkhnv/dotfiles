# Specs and plans stay local: they work through the threat model in far more
# detail than the code does, and none of that detail is needed to read the
# repository. The rest of docs/ is published.
docs/superpowers/

# SDD scratch: ledgers, briefs and review packages for the plans above.
.superpowers/
