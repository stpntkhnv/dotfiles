---
covers:
  features: []
  paths: []
---

# Реестр обходов

Этот репозиторий время от времени обходит поведение чужой программы: баг,
недокументированную особенность или просто то, что дистрибутив не выправил.
Обход без записи выглядит как часть логики репозитория и переживает баг,
который его вызвал, на годы дольше необходимого. Эта таблица существует,
чтобы у каждого обхода был путь назад: доказательство, что баг чужой, и
проверка, по которой видно, что баг всё ещё жив. Когда проверка перестаёт
подтверждаться — обход можно снять, а строку удалить.

| Обход | Чей баг | Доказательство | Как проверить, что баг ещё жив | Документ |
|---|---|---|---|---|
| distrobox бинд-монтирует `/mnt` (и `/media`, `/run/media`, `/var/mnt`) поверх пользовательского тома при каждом старте контейнера, поэтому том с сокетом контекста живёт в `/var/lib/wsproxy`, а не в `/mnt` | `distrobox` (`89luca89/distrobox`) | Не найдено на дату: upstream не найден, искали 2026-07-30 — issue-трекер и обсуждения (в т.ч. #778, #1020, #1527, #2036) описывают смежные, но другие проблемы с точками монтирования. Механизм подтверждён исходником: `distrobox-init` (тег 1.8.2.5, тот же, что в `extra` Arch на дату проверки) держит `/mnt` в списке `HOST_MOUNTS` и монтирует его безусловно на каждом старте циклом `for host_mount in ${HOST_MOUNTS}; do mount_bind ...`, без флага исключить отдельный путь | внутри контейнера положить том на `/mnt` вместо `/var/lib/...`, перезапустить контейнер (`distrobox stop <name> && distrobox enter <name>`) и проверить `mount \| grep ' /mnt '` — если видно смонтированный хостовый `/mnt`, а не свой том, баг жив | [containers.md](containers.md) |
| Образ `docker.io/library/archlinux:latest` в коде репозитория считается идущим с пустым `mirrorlist` и без `Include` для `[extra]` — `run_onchange_before_10-bootstrap-pacman` чинит оба перед первым `pacman -Syu` | `archlinux/archlinux-docker` — по проверке 2026-07-30 не воспроизводится | Наш замер: `podman run --rm --pull=always docker.io/library/archlinux:latest sh -c 'cat /etc/pacman.d/mirrorlist'` 2026-07-30 вернул уже заполненный список (`fastly.mirror.pkgbuild.com`, `geo.mirror.pkgbuild.com`), и `[extra]` в `/etc/pacman.conf` того же образа уже содержит `Include = /etc/pacman.d/mirrorlist`. История файла `rootfs/etc/pacman.d/mirrorlist` в `archlinux/archlinux-docker` показывает рабочий мирror с 2017 года, нынешнюю пару — с правки 2022 года | тот же `podman run --rm --pull=always docker.io/library/archlinux:latest sh -c "cat /etc/pacman.d/mirrorlist"` — пустой вывод или отсутствие `Include` под `[extra]` в `/etc/pacman.conf` означает, что баг вернулся | [containers.md](containers.md) |
| `podman info` вываливается с «kernel does not support overlay fs» после обновления пакета `linux`, пока машина не перезагружена | пакетирование ядра Arch (не баг конкретно `podman`) | Тикет upstream: `bugs.archlinux.org` FS#23809 (2011, закрыт как Won't Fix — «the only sensible solution is to not do an upgrade if you are not prepared to reboot») и FS#16702 (2009, перенесён в GitLab без отдельного решения); FS#73043 (2021) закрыт как дубликат FS#16702. Модуль `overlay` собран как `.ko`, не встроен в ядро (`CONFIG_OVERLAY_FS=m`, подтверждено на этой машине), поэтому пропадает вместе с прочими модулями работающего ядра, когда пакет `linux` удаляет их каталог при обновлении | `uname -r` расходится с `pacman -Q linux`, и `grep -c overlay /proc/filesystems` возвращает `0` — перезагрузка чинит | [containers.md](containers.md) |
| Скрипт 32 кладёт `policies.json` для Zen в `/etc/zen/policies/`, а не в буквальный `/etc/firefox/policies/` | Не баг — так работает Gecko у любой сборки на его основе, включая форки (`zen-browser-bin`); официальная документация Mozilla говорит только про Firefox | Документация upstream (через исходник — `support.mozilla.org` и `firefox-admin-docs.mozilla.org` сами называют только буквальный `/etc/firefox/policies` и ни словом не упоминают форки): `toolkit/components/enterprisepolicies/EnterprisePoliciesParent.sys.mjs`, метод `_getLocalConfigurationFile`, берёт каталог по ключу `SysConfD`; `xpcom/io/SpecialSystemDirectory.cpp`, функция `GetUnixSystemConfigDir`, разворачивает `SysConfD` в `/etc/<Name из application.ini этой сборки, в нижнем регистре>` — не `RemotingName`, как можно было бы подумать по аналогии с реестром Windows. На этой машине оба поля лексически совпадают после lowercase: `grep -E '^(Name\|RemotingName)=' /opt/zen-browser-bin/application.ini` (2026-07-30) → `Name=Zen`, `RemotingName=zen` | `grep -E '^(Name\|RemotingName)=' /opt/zen-browser-bin/application.ini` — пока оба поля лексически дают `zen` в нижнем регистре, обход актуален; если `Name` разойдётся (смена бренда/сборки Zen), реальный каталог политики — `/etc/<новый Name в нижнем регистре>/policies/`, и `run_onchange_before_32-browser-extensions.sh.tmpl` нужно поправить вручную | [isolation-browser.md](isolation-browser.md) |
| Прокси на контейнер раздаёт собственное расширение `context-proxy`, а не встроенная «Advanced proxy settings» в Multi-Account Containers | `mozilla/multi-account-containers` — для схемы `socks5` официально не баг; необязательное право `proxy` — намеренный дизайн проекта | Тикет upstream: `github.com/mozilla/multi-account-containers/issues/2669` «Cannot use socks5:// URIs», закрыт мейнтейнером `bakulf` 2024-09-27 как invalid — «The right scheme for Socks5 URLs is `socks` and not `socks5`»; регулярка `parseProxy` (`src/js/proxified-containers.js`), допускающая только `http`/`https`/`socks`/`socks4`, подтверждена чтением исходника. Право `proxy` — в `optional_permissions`, не `permissions` (`src/manifest.json`); без него панель `advanced-proxy-settings-panel` (`src/js/popup.js`, метод `prepare()`) блокирует все поля (`el.disabled = true`) и показывает оверлей с кнопкой «Enable» — задокументировано в wiki `Permissions` того же репозитория | Открыть Multi-Account Containers → любой контейнер → Advanced proxy settings, ввести `socks5://127.0.0.1:11081` и нажать Apply to Container — если значение сохранится и заработает, парсер расширили и обход можно снять; отдельно проверить в `about:addons` → расширение → Permissions, не стало ли право `proxy` обязательным (`permissions`) | [isolation-browser.md](isolation-browser.md) |
| Конфиг `context-proxy` зашит прямо в собранный XPI, а установка идёт по `file:///usr/local/lib/zen-context-proxy.xpi` без отдельной политики `storage.managed` | Не баг, а полезное поведение upstream, на которое репозиторий опирается — `mozilla-firefox/firefox` (Gecko) | Документация upstream (через исходник): `toolkit/components/enterprisepolicies/PoliciesHelpers.sys.mjs`, функция `installAddonFromURL` — условие `!addon.sourceURI.schemeIs("file")` намеренно исключает адреса `file:` из раннего выхода «тот же аддон, не переустанавливать»; более поздняя проверка версии в той же функции (`Services.vc.compare(addon.version, install.addon.version) == 0` → «Installation cancelled because versions are the same») добавлена исправлением `bugzilla.mozilla.org/show_bug.cgi?id=1715965` (Firefox 92) именно для того, чтобы не переустанавливать `file:`-расширения без нужды — то есть оба условия задокументированы в самом коде как намеренные | Открыть `searchfox.org/mozilla-central/source/toolkit/components/enterprisepolicies/PoliciesHelpers.sys.mjs`, найти `installAddonFromURL` и убедиться, что условие `!addon.sourceURI.schemeIs("file")` всё ещё есть — уберут его, и переустановка при пересборке XPI перестанет гарантированно срабатывать | [isolation-browser.md](isolation-browser.md) |
| Расширение `Open external links in a container` ставится `force_installed` версией `1.0.3` без проверки, что вызвавшая ссылку страница имела право выбирать контейнер | Не баг, а состояние проекта на эту дату — `honsiorovskyi/open-url-in-container`: подпись разработана только в незавершённой ветке | Документация upstream (через исходник и релиз): тег `1.0.3` — `src/manifest.json` без `options_ui`, `src/opener.js` без единого упоминания `signature`, читает `name`/`id` из ссылки `ext+container:` напрямую; ветка `2.0.0alpha10` (`README.md`, раздел «What is signature?») описывает HMAC-SHA256, добавленный только там; `addons.mozilla.org/api/v5/addons/addon/open-url-in-container/` подтверждает `current_version` = `1.0.3` на 2026-07-30 — подписи нет ни в одной версии, которую можно поставить с AMO | `curl -s https://addons.mozilla.org/api/v5/addons/addon/open-url-in-container/ \| python3 -c "import json,sys; print(json.load(sys.stdin)['current_version']['version'])"` — пока возвращает `1.0.3`, обход актуален; если версия выросла до `2.x`, проверить, появился ли в `manifest.json` `options_ui` и обязательный параметр `signature` в `opener.js` | [isolation-browser.md](isolation-browser.md) |
| Ссылка, кликнутая изнутри рабочего контейнера, уходит на хост через `distrobox-host-exec`/`host-spawn` без учёта того, что оба требуют `flatpak-session-helper` на хосте — сейчас на этой машине оба возвращают код `127` без единой строки вывода, отказ полностью молчаливый | `distrobox` (`89luca89/distrobox`) и `host-spawn` (`1player/host-spawn`) — необъявленная жёсткая зависимость от пакета `flatpak`, которого нет ни в списке зависимостей самого distrobox, ни в документации host-spawn | Тикет upstream: `github.com/89luca89/distrobox/issues/1692` «flatpak on host requirement not documented?» (открыт, автор `45mg`) — прямо говорит, что `distrobox-host-exec` требует `flatpak` на хосте и падает молча без него, нигде это не документируя; официальная страница `distrobox.it/usage/distrobox-host-exec/` называет единственной зависимостью `host-spawn` и нигде не документирует зависимость от `flatpak` как требование на стороне хоста — `flatpak` встречается там только внутри примера использования (`distrobox-host-exec flatpak run org.mozilla.firefox`). Наш замер 2026-07-30, воспроизведено на `digi3`/`stellium`/`personal`: `distrobox enter digi3 -- distrobox-host-exec echo hi` → пусто, `rc=127`; `pacman -Q flatpak` на хосте → «package 'flatpak' was not found»; `dbus-send --session --dest=org.freedesktop.Flatpak.Development /org/freedesktop/Flatpak/Development org.freedesktop.DBus.Peer.Ping` на хостовой шине напрямую (без контейнера) → `Error org.freedesktop.DBus.Error.ServiceUnknown: The name is not activatable`; исходник `host-spawn` (`command.go`) стучится именно в `org.freedesktop.Flatpak.Development.HostCommand` с комментарием «talk with flatpak-session-helper process» — сервиса регистрировать некому, раз `flatpak` не установлен | `distrobox enter <контекст> -- distrobox-host-exec id -un` — пустой вывод и код `127` означают, что баг жив; `pacman -Q flatpak` на хосте — «not found» подтверждает причину; если баг пофиксили или `flatpak` появился в зависимостях/фичах, эта же команда должна напечатать имя пользователя | [isolation-links.md](isolation-links.md) |
| `HERDR_AGENT=claude` ставится на самой команде `distrobox enter` (алиасы, `work`), а не через `export` внутри контейнерного шелла и не в конфиге `claude` | Не баг — задокументированное поведение `herdr` (`herdrdev/herdr`) | Документация upstream: `herdr.dev/docs/agents/`, раздел «VMs and Sandbox Wrappers» — «Set `HERDR_AGENT=<agent>` on the wrapper command... The hint is scoped to that foreground process; setting it only inside a VM or container is not visible to Herdr, and you should avoid exporting it globally». Наш замер той же природы — [issues/2026-07-30-herdr-agent-detection-in-containers.md](issues/2026-07-30-herdr-agent-detection-in-containers.md), раздел 2: `export HERDR_AGENT=claude` внутри контейнерного шелла даёт `agent=None`, `unknown`, потому что herdr читает окружение процесса, который сам запустил (`distrobox`/`podman` на хосте), а не окружение процесса за границей pid namespace контейнера | Изнутри уже открытой панели контейнера выполнить `export HERDR_AGENT=claude` и посмотреть `herdr agent explain <pane>` с хоста — пока агент остаётся `agent_not_found`/`None`, обход нужен | [multiplexer.md](multiplexer.md) |
| Статус агента в контейнерной панели берётся только через метку `HERDR_AGENT` плюс скрининг экрана; пуш состояния по сокету (`pane.report_agent`) в `work` и в алиасах сознательно не используется | Не баг — задокументированное поведение `herdr` | Документация upstream: `herdr.dev/docs/agents/`, раздел «Status Authority» — «For agents with complete lifecycle hooks, the integration is authoritative when it is installed and actively reporting for the running pane... It does not also run screen manifest fallback for that same lifecycle authority». Наш замер — [issues/2026-07-30-herdr-agent-detection-in-containers.md](issues/2026-07-30-herdr-agent-detection-in-containers.md), раздел 3: отрепорченный `idle` держался в `agent list`/`pane.get`, пока экран панели уже показывал `working`; `pane.release_agent` и `pane.clear_agent_authority` не возвращают состояние экрану, а снимают агента целиком (`agent=None`); на отрепорченной панели `agent prompt` дополнительно отказывает с `agent_not_ready` | Изнутри контейнера отрепортить `pane.report_agent` со статусом `idle` на панель с активно работающим агентом, затем сравнить `herdr agent list` с содержимым экрана панели — пока `agent list` не меняется вслед за экраном, обход нужен | [multiplexer.md](multiplexer.md) |
| Скрипты этого репозитория не используют форму `--source id:sub` (двоеточие ломает разбор), везде, где нужен `<PANE_ID>`, ставят его первым аргументом вопреки usage-строке `--help`, и не доверяют коду возврата `0` как признаку успеха для команд, пишущих не в терминал | `herdr` (`herdrdev/herdr`), CLI версии 0.7.5 | Наш замер — [issues/2026-07-30-herdr-agent-detection-in-containers.md](issues/2026-07-30-herdr-agent-detection-in-containers.md), раздел 4. Перепроверено на этой машине 2026-07-31: `herdr --version` → `0.7.5`; `herdr pane report-agent --help` печатает `Usage: herdr pane report-agent [OPTIONS] --source <ID> --agent <LABEL> --state <STATUS> <PANE_ID>` — `<PANE_ID>` последним, при этом сайт `herdr.dev/docs/cli-reference/` уже показывает `<pane_id>` первым: встроенный `--help` и сайт разошлись. Апстрим-поиск (issues и `CHANGELOG.md` репозитория `herdrdev/herdr`) отдельного тикета про это несовпадение не нашёл | `herdr pane report-agent --help` — пока `<PANE_ID>` в строке `Usage:` стоит последним, а не первым, несовпадение живо | [multiplexer.md](multiplexer.md) |
| Все три алиаса контекста и `work` входят в контейнер флагом `--no-workdir` | Не баг — задокументированное поведение distrobox (`89luca89/distrobox`) по умолчанию | Документация upstream: `distrobox.it/usage/distrobox-enter/` — `--no-workdir`/`-nw`: «always start the container from container's home directory», то есть без флага рабочий каталог хоста переносится внутрь через `/run/host`. Наш замер (`home/dot_bashrc.tmpl`, комментарий над блоком алиасов): `distrobox enter digi3 -- pwd` → `/run/host/home/stsiapan`; `distrobox enter --no-workdir digi3 -- pwd` → `/home/stsiapan/homes/digi3` | `distrobox enter <контекст> -- pwd` без флага — пока путь начинается с `/run/host`, а не с домашнего каталога контекста, обход нужен | [multiplexer.md](multiplexer.md) |
| Не полагаемся на панель сессий DankMaterialShell как на способ увидеть или переключить сессию herdr — `~/.tmux.conf` и `-tmux`-двойники алиасов остаются единственным путём, который эта панель понимает | Не баг — нереализованная фича `DankMaterialShell` (`AvengeMedia/DankMaterialShell`): панель сессий ищет только `tmux`/`zellij` | Не найдено на дату: upstream не найден, искали 2026-07-31 — ни в issues, ни в `CHANGELOG`/релизах репозитория не нашлось тикета с запросом поддержки herdr в панели сессий. Механизм подтверждён исходником: `quickshell/Services/MuxService.qml` — процессы `tmuxCheckProcess`/`zellijCheckProcess` определяют доступность мультиплексора вызовом `command -v tmux`/`command -v zellij` и проверкой кода возврата; herdr там не упомянут нигде | Прочитать `quickshell/Services/MuxService.qml` в `AvengeMedia/DankMaterialShell` (или поискать `herdr` по всему репозиторию) — пока там только `command -v tmux`/`command -v zellij`, панель herdr не увидит | [multiplexer.md](multiplexer.md) |
| Биндинги диктовки (`home/dot_config/niri/voice.kdl.tmpl`) вызывают `handy --toggle-transcription`/`--toggle-post-process` через `spawn`, а не шлют `pkill -USR2`/`-USR1` документированным Handy способом | `cjpais/Handy` — сборщик мусора JavaScriptCore (встроен через WebKitGTK) занимает `SIGUSR1` для приостановки своих потоков, и обработчик Handy на том же сигнале конфликтует с ним в обе стороны; `SIGUSR2` падает отдельно, по другому багу | Тикет upstream: [cjpais/Handy#1660](https://github.com/cjpais/Handy/issues/1660) («WebKit's GC triggers phantom recordings — SIGUSR1 remote control clashes with webkit2gtk thread suspension», открыт 2026-07-11; независимое подтверждение в комментарии `sasha01zuev` от 2026-07-28 — 10 уничтоженных диктовок из 26 за одно утро, каждая оборвана на 122–123 секунде) и [cjpais/Handy#512](https://github.com/cjpais/Handy/issues/512) («pkill -USR2 -n handy crash the handy», открыт). Фикс — [cjpais/Handy#1267](https://github.com/cjpais/Handy/pull/1267) (SIGRTMIN+1/+2 вместо SIGUSR1/2), открыт с 2026-04-10, на 2026-07-31 не смёржен и не входит в 0.9.4. Наш замер: `coredumpctl list handy` на этой машине показывает шесть падений `SIGSEGV`, все 2026-07-29, с идентичным кадром `#0` в `libjavascriptcoregtk-4.1.so.0` — воспроизведено детерминированно и записано в коммите `4ce70ad` этого репозитория | Проверить, смёржен ли [pull/1267](https://github.com/cjpais/Handy/pull/1267) и вошёл ли в актуальный релиз `handy-bin` — пока нет, сигналы остаются небезопасными и обход нужен | [voice.md](voice.md) |
| Печать распознанного текста идёт порциями по 12 символов через `wtype -d 1` (`home/bin/executable_handy-type.sh`) вместо одного вызова на весь текст | `atx/wtype` — виртуальная раскладка клавиатуры, которую `wtype` строит из уникальных символов текста по порядку появления, на стеке niri/ghostty теряет 14-й и 15-й уникальный символ целиком | Наш замер (комментарий в самом скрипте): 88 прогонов в сфокусированное окно, побайтовое сравнение — порция 12 не теряла ничего ни при какой задержке между клавишами, порция 64 теряла ровно два символа в каждом прогоне. Частичное подтверждение апстримом: [atx/wtype#71](https://github.com/atx/wtype/issues/71) («Punctuation at 14th unique character position in Chromium/Electron», открыт) — тот же механизм (виртуальная раскладка вокруг служебных keycode), но другое окружение (Chromium/Electron, не niri/ghostty) и только 14-я позиция — 15-я у автора issue печаталась нормально | Продиктовать в фокусированное текстовое поле строку длиннее 14 уникальных символов без ограничения на 12-символьные порции и сверить побайтово — если 14-й/15-й уникальный символ пропадает, обход всё ещё нужен (не выполнялось в рамках этой задачи — `wtype` печатает в реальное активное окно) | [voice.md](voice.md) |
| Тот же скрипт схлопывает любую последовательность из `\n`, `\r`, `\t` (с пробелами по краям) в один пробел перед печатью | `atx/wtype`/`libxkbcommon` — `wtype` не различает текст и нажатия клавиш, а `U+000A` отображается на keysym `Linefeed`, который приложение получает тем же байтом `0x0A`, что и `Ctrl+J`; терминальная строка ввода читает этот байт как Enter | Не найдено на дату: upstream не найден, искали 2026-07-31 — отдельного тикета в `atx/wtype` про `U+000A`/`Linefeed`/Enter не нашлось; похоже, это не баг с точки зрения проекта, а прямое следствие таблицы keysym X11/XKB, где `Linefeed` документирован как соответствие `U+000A` независимо от `wtype`. Наш замер — история реального случая в комментарии скрипта (диктовка, разбитая моделью вычитки на три абзаца, ушла в Claude Code двумя кусками) и свежая проверка `~/.local/share/com.pais.handy/history.db` на этой машине 2026-07-31: 0 из 78 сырых расшифровок содержат перевод строки, 2 из 41 очищенных вычиткой — внутренний (не только завершающий) | Продиктовать текст, который вычитка модели разобьёт на абзацы, с отключённым схлопыванием пробелов — если перевод строки посреди текста доходит до терминала как Enter, обход нужен (не выполнялось в рамках этой задачи — способ проверки исполняет `wtype` в реальное активное окно) | [voice.md](voice.md) |
| Каталог фич (`home/.chezmoidata.yaml`, ключ `voice`) явно перечисляет `openblas` в `pacman`, хотя это обычная зависимость AUR-пакета `handy-bin`, а не что-то специфичное для этого репозитория | `handy-bin` 0.9.4 линковался с `libopenblas.so.0`, не объявляя его в зависимостях — падал при запуске без явной установки пакета | Тикеты upstream: [cjpais/Handy#1611](https://github.com/cjpais/Handy/issues/1611) (Ubuntu/Debian, 2026-07-05), [cjpais/Handy#1615](https://github.com/cjpais/Handy/issues/1615) (Fedora, другое имя пакета, 2026-07-06), более старый [cjpais/Handy#42](https://github.com/cjpais/Handy/issues/42); апстрим смёржил фикс — [cjpais/Handy#1621](https://github.com/cjpais/Handy/pull/1621), «add openblas to ci and packaging for linux» (смёржен 2026-07-07). Наш замер 2026-07-31: `pacman -Qi handy-bin` на этой машине и метаданные AUR (`aur.archlinux.org/rpc/v5/info/handy-bin`) показывают `openblas` уже в `Depends On`/`Depends` самого пакета версии `0.9.4-2`, с `LastModified` от 2026-07-29 — позже, чем правка `.chezmoidata.yaml` (коммит `ad6a10e`, 2026-07-26), которая описала зависимость как необъявленную | `pacman -Qi handy-bin \| grep -o openblas` (или `curl -s https://aur.archlinux.org/rpc/v5/info/handy-bin \| jq '.results[0].Depends'`) — раз `openblas` уже виден в зависимостях пакета, обход, похоже, избыточен для актуальной сборки, но остаётся безвредным щитом на случай регресса пакета | [voice.md](voice.md) |
| Биндинги диктовки живут в `home/dot_config/niri/voice.kdl.tmpl` и вызывают `spawn "handy" "--toggle-..."` из конфига композитора, а не полагаются на глобальные горячие клавиши самого приложения | `tauri-apps/tauri` (плагин `global-shortcut`) — не умеет захватывать клавиши на композиторах в духе wlroots, включая niri | Тикеты upstream: [tauri-apps/tauri#3578](https://github.com/tauri-apps/tauri/issues/3578) («Global Shortcut support on Wayland», открыт) и трекинг-issue [tauri-apps/global-hotkey#28](https://github.com/tauri-apps/global-hotkey/issues/28) (открыт) в библиотеке, которую использует плагин. Прямое подтверждение на самом Handy — [cjpais/Handy#949](https://github.com/cjpais/Handy/issues/949), «Global shortcuts don't work on Wayland compositors (COSMIC, Sway, Hyprland)» (закрыт), называет тот же недостающий протокол `zwp_keyboard_shortcuts_inhibit_manager_v1` и подтверждает, что CLI-флаг `--toggle-transcription` работает как обход | Проверить, реализован ли захват глобальных клавиш на wlroots-композиторах в [tauri-apps/global-hotkey#28](https://github.com/tauri-apps/global-hotkey/issues/28) — пока issue открыт, обход (биндинг на стороне композитора) нужен | [voice.md](voice.md) |

## Состояния доказательства

Столбец «Доказательство» принимает ровно одно из четырёх состояний. Третье и
четвёртое так же законны, как первые два — отсутствие тикета не повод
выдумывать его или молчать об обходе.

### Тикет upstream

Нашёлся issue или pull request в трекере чужого проекта, ссылка на него.

### Документация upstream

Поведение описано в официальной документации чужого проекта и не считается
багом с его точки зрения. Пример: `--no-convert` у `localectl`, `MOZ_APP_NAME`
у Zen.

### Наш замер

Тикета в чужом трекере нет, но есть воспроизведение — либо в
`docs/issues/`, либо прямо в теле документа, который описывает обход.

### Не найдено на дату

Искали и не нашли. Пишется буквально, с датой поиска: «upstream не найден,
искали 2026-07-30». Это не повод убрать обход из таблицы: он остаётся с
честной пометкой, и следующий, кто ищет, знает, что поиск уже был.
