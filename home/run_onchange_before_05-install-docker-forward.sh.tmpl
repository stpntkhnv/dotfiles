{{ if .setup_docker_forward }}
#!/bin/bash
set -euo pipefail

if [[ -z "${CONTAINER_ID:-}" ]]; then
    exit 0
fi

echo "Installing socat for Docker port forwarding..."
sudo pacman -S --noconfirm --needed socat

echo "Docker forward setup complete"
{{ end }}
